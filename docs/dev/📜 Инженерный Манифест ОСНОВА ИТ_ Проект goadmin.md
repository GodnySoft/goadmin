# **📜 Инженерный Манифест ОСНОВА ИТ: Проект `goadmin`**

**Наше Кредо:** Автоматизация — это дисциплина. Если систему нельзя воспроизвести — её нет. Linux — это платформа. Go — инструмент системной автоматизации. Архитектура — это наша ответственность.

## **1\. Идея и Миссия платформы**

Современная инфраструктура часто страдает от «зоопарка» bash-скриптов, ручных правок на серверах и разрозненных систем мониторинга.

**Наша миссия** — уничтожить хаос.

Мы создаем **`goadmin`** — универсального, строго типизированного и безопасного агента управления инфраструктурой. Это единый бинарный файл, который разворачивается на серверах Ubuntu 24.04 LTS и берет на себя рутину: сбор метрик, управление контейнерами, контроль сети и обеспечение безопасности. Он выступает и как локальная CLI-утилита для инженера, и как фоновый демон, подчиняющийся командам из корпоративных мессенджеров (Max, Telegram) и Web-интерфейса.

## **2\. Архитектурные принципы**

Мы строим систему, которая должна пережить нас и легко масштабироваться.

* **Модульный монолит (Ports & Adapters):** Ядро системы ничего не знает о том, откуда пришла команда (терминал, бот или REST API). Вся бизнес-логика изолирована от интерфейсов.  
* **Zero-Trust (Нулевое доверие):** Любая команда, приходящая извне, считается потенциально опасной. Агент жестко авторизует источники (по белому списку ID) и валидирует входные данные до передачи их в системный вызов (`os/exec`).  
* **Воспроизводимость:** Сервер — это расходный материал (Cattle, not Pets). Если сервер падает, мы не чиним его руками, мы разворачиваем его заново через Ansible, который автоматически устанавливает `goadmin`.  
* **Отказоустойчивость агента:** Утечки памяти недопустимы. Агент должен работать месяцами без перезагрузки. Все горутины жестко контролируются через `context.Context`. Graceful Shutdown обязателен.

## **3\. Сущности и Бизнес-процессы**

Чтобы говорить на одном языке, мы фиксируем единую терминологию (Ubiquitous Language):

* **Узел (Node):** Физический или виртуальный сервер под управлением Ubuntu 24.04 LTS. Единственная целевая платформа.  
* **Агент (Agent):** Запущенный экземпляр приложения `goadmin` в режиме демона (systemd-сервис).  
* **Транспорт (Transport):** Канал связи. То, как пользователь взаимодействует с Агентом (CLI, HTTP/REST API, Telegram Bot, MaxBot).  
* **Модуль (Module):** Изолированный блок бизнес-логики (например, модуль `host` для метрик, модуль `docker` для контейнеров).  
* **Команда (Command):** Стандартизированный запрос от Транспорта к Модулю.

**Ключевые процессы платформы:**

1. **Observability (Наблюдаемость):** Агент в фоне (через планировщик) опрашивает модули, собирает телеметрию и пишет её во встроенную БД SQLite.  
2. **Fleet Management:** Удаленное исполнение диагностических и восстановительных команд через защищенные каналы связи без необходимости открывать SSH-сессии всем инженерам.  
3. **Auditing:** Каждое действие, выполненное через Транспорт, логируется локально в `journald` в формате JSON и реплицируется в центральное хранилище.

## **4\. Технологии и Автоматизация**

Мы используем прагматичный и мощный стек, минимизируя внешние зависимости:

* **Golang (1.22+):** Наш основной язык. Идеален для системного программирования, компилируется в один бинарный файл, обладает мощными встроенными инструментами конкурентности и профилирования.  
* **Ubuntu 24.04 LTS:** Жесткий стандарт базовой ОС. Мы максимально используем её нативные возможности: `systemd` для изоляции процессов (cgroups v2, namespaces), `journald` для логов, `ufw/nftables` для сети.  
* **SQLite:** Локальная, встроенная прямо в бинарник (или через CGO) база данных. Никаких внешних СУБД для хранения локальных метрик агента.  
* **Embedded UI (`go:embed`):** Frontend запаковывается прямо в бинарник `goadmin`. Нам не нужен Nginx для того, чтобы показать инженеру локальный дашборд.

**Автоматизация всего:** Сборка агента, линтинг (с параноидальными правилами `golangci-lint`), тестирование и релиз осуществляются исключительно через CI/CD. Развертывание на серверах производится идемпотентными Ansible-плейбуками.

## **5\. Документация и Подход к разработке**

Документация — это не то, что пишется "когда-нибудь потом". Это часть инженерной культуры.

* **Documentation as Code:** Вся документация живет рядом с кодом в репозитории (в формате Markdown).  
* **ADR (Architecture Decision Records):** Любое значимое архитектурное решение (выбор библиотеки, изменение структуры БД) фиксируется в виде короткого документа: *Контекст \-\> Варианты \-\> Решение \-\> Последствия*.  
* **Комментарии в коде:** Пишутся на русском языке. Код должен быть написан так чисто (осмысленные имена переменных, отсутствие магии), чтобы объяснять *что* он делает не пришлось. Комментарии объясняют **почему** было принято то или иное решение.  
* **Контрактная разработка:** Сначала мы проектируем интерфейсы (Interfaces) в слое `core`, утверждаем их, и только потом пишем реализацию в модулях.

## **6\. Приверженность Свободному ПО (Open Source)**

Вся наша инфраструктура строится на плечах гигантов Open Source сообщества: Linux, GNU, Go, SQLite.

* **Использование:** Мы отдаем приоритет открытым, проверенным временем и сообществом инструментам, избегая проприетарных "черных ящиков", которые мы не можем профилировать или починить.  
* **Безопасность через прозрачность:** Открытый исходный код базовых технологий позволяет нам проводить глубокий аудит (SBOM, Dependency Scanning).  
* **Вклад:** Если в процессе разработки мы создаем независимые утилиты (например, улучшенные обертки над `gopsutil` или парсеры логов Ubuntu), мы рассматриваем возможность их выделения в открытые репозитории для поддержки сообщества.

---

Документ описывает наш "mindset". Если разработчик или инженер разделяет этот манифест, он сможет писать качественный код для `goadmin` и строить надежную инфраструктуру вместе с нами.